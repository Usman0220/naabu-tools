[package]
name = "naabu-cli"
version = "0.1.0"
edition = "2021"
description = "CLI: generate random public IPs and scan them with naabu"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
rand = "0.8"